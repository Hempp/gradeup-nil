{
  "name": "gradeup-nil",
  "version": "1.0.0",
  "description": "NIL platform connecting student-athletes with brands based on academic excellence",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run",
    "test:coverage": "vitest --coverage",
    "firebase:serve": "npx firebase-tools emulators:start --only hosting",
    "firebase:deploy": "npm run build && npx firebase-tools deploy --only hosting",
    "firebase:deploy:preview": "npm run build && npx firebase-tools hosting:channel:deploy preview",
    "firebase:login": "npx firebase-tools login"
  },
  "devDependencies": {
    "@vitest/ui": "^4.0.18",
    "firebase-tools": "^15.5.1",
    "happy-dom": "^20.5.0",
    "jsdom": "^28.0.0",
    "supabase": "^2.76.4",
    "vite": "^5.4.0",
    "vitest": "^4.0.18"
  }
}
