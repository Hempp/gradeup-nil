# =============================================================================
# GradeUp NIL - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration (Required)
# -----------------------------------------------------------------------------
# Get these from: https://supabase.com/dashboard/project/_/settings/api

# Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Your Supabase anonymous (public) key
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# -----------------------------------------------------------------------------
# Supabase Admin (Optional - for server-side admin operations)
# -----------------------------------------------------------------------------
# Only needed for admin functionality. Keep this secret!
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# -----------------------------------------------------------------------------
# Sentry Error Monitoring (Optional but recommended for production)
# -----------------------------------------------------------------------------
# Get DSN from: https://sentry.io/settings/[org]/projects/[project]/keys/

# Sentry DSN for client-side (browser) error tracking
# NEXT_PUBLIC_SENTRY_DSN=https://your-key@sentry.io/your-project-id

# Sentry DSN for server-side error tracking
# SENTRY_DSN=https://your-key@sentry.io/your-project-id

# Auth token for source map uploads (optional, for better stack traces)
# SENTRY_AUTH_TOKEN=your-auth-token

# Organization and project slugs for source map uploads
# SENTRY_ORG=your-org-slug
# SENTRY_PROJECT=your-project-slug

# -----------------------------------------------------------------------------
# Demo Mode (Development Only)
# -----------------------------------------------------------------------------
# IMPORTANT: Set to 'false' or remove entirely in production!
# Demo mode bypasses authentication and shows mock data instead of real Supabase data.
# SKIP_AUTH_CHECK=true
# NEXT_PUBLIC_DEMO_MODE=true

# For production, use:
SKIP_AUTH_CHECK=false
NEXT_PUBLIC_DEMO_MODE=false

# -----------------------------------------------------------------------------
# Application Settings (Optional)
# -----------------------------------------------------------------------------
# Override the default site URL (useful for preview deployments)
# NEXT_PUBLIC_SITE_URL=https://your-domain.com

# App version for release tracking
# NEXT_PUBLIC_APP_VERSION=1.0.0

# -----------------------------------------------------------------------------
# Analytics (Optional)
# -----------------------------------------------------------------------------
# Enable/disable analytics tracking
# NEXT_PUBLIC_ANALYTICS_ENABLED=true

# Google Analytics 4 Measurement ID (optional)
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Custom analytics endpoint for self-hosted analytics (optional)
# NEXT_PUBLIC_ANALYTICS_ENDPOINT=https://analytics.yourdomain.com/collect

# -----------------------------------------------------------------------------
# Web Push Notifications (Optional)
# -----------------------------------------------------------------------------
# Generate VAPID keys with: npx web-push generate-vapid-keys

# VAPID public key (safe to expose to client)
VAPID_PUBLIC_KEY=

# VAPID private key (keep secret!)
VAPID_PRIVATE_KEY=

# Contact email for push service (required for VAPID)
VAPID_SUBJECT=mailto:support@gradeupnil.com

# -----------------------------------------------------------------------------
# Email (Resend)
# -----------------------------------------------------------------------------
# Get API key from: https://resend.com/api-keys
# RESEND_API_KEY=re_xxxxxxxxxxxx
# EMAIL_FROM=GradeUp NIL <noreply@gradeupnil.com>

# -----------------------------------------------------------------------------
# Stripe Payment Processing (Optional)
# -----------------------------------------------------------------------------
# Get these from: https://dashboard.stripe.com/apikeys

# Stripe secret key (keep this secret!)
STRIPE_SECRET_KEY=sk_test_xxxx

# Stripe publishable key (safe for client-side)
STRIPE_PUBLISHABLE_KEY=pk_test_xxxx

# Stripe webhook secret for verifying webhook signatures
# Get this from: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_xxxx
